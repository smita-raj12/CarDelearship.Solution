@using CarDelear.Models;

<h1>Car Delearship</h1>
@if (Model.Count == 0)
{
  <p>There are no cars in the list.</p>
}
<ul>
  @foreach (Car i in Model)
  {
    <li>@i.MakeModel</li>
    <li>@i.Price</li>
    <li>@i.Miles</li>
  }
</ul>

<a href="/cars/new">Add a new Car.</a>